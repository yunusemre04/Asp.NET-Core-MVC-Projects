@{
    ViewData["Title"] = "Yeni Şifre";
}

<div class="text-center mt-5">
    <h4>✅ Şifre sıfırlama başarılı!</h4>
    <p class="mt-3">@Html.Raw(ViewBag.Message)</p>
    <a asp-action="Login" class="btn btn-success mt-4">Giriş Yap</a>
</div>

