@{
    ViewData["Title"] = Localizer["PageTitle"];
}

<div class="text-center mt-5">
    <h4>✅ @Localizer["SuccessHeader"]</h4>
    <p class="mt-3">@Html.Raw(ViewBag.Message)</p>
    <a asp-action="Login" class="btn btn-success mt-4">@Localizer["LoginButton"]</a>
</div>